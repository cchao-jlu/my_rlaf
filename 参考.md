Event-Trace Distilled Slow-Fast Guidance for CDCL SAT Solving基于求解轨迹蒸馏的快慢协同 CDCL SAT 求解引导方法📑 1. 核心贡献 (Core Contributions)在将机器学习（尤其是图神经网络）引入 CDCL SAT 求解器的研究中，现有的神经求解器普遍面临三大核心瓶颈：静态 GNN 的表达力瓶颈 (Expression Bottleneck of Static GNNs): 受限于 $1\text{-WL}$ (Weisfeiler-Lehman) 检验，基于消息传递的图神经网络 (MPNN) 无法自发打破高度同构/对称图结构的对称性。GPU 推理的延迟开销 (GPU Inference Latency): 频繁地（如在每个决策分支或多轮精炼中）调用大模型/GNN 会带来难以承受的 Wall-clock time 开销。强化学习的奖励稀疏性 (Sparse Reward in Reinforcement Learning): 以最终是否解出 (Solved) 或求解时间作为 Reward，反馈极其稀疏且延迟，导致强化学习难以收敛。为此，本项目提出了一种全新的 Slow-Fast 神经引导框架，核心贡献如下：打破表达力上限 (Search-Induced Symmetry Breaking): 首次提出利用 Solver 动态搜索轨迹 (Execution Trace) 作为天然、廉价的动态标识符 (Dynamic Identifiers)。由于对称变量在搜索树中所处的动态上下文不同，通过轨迹特征可以自发且动态地打破静态图的同构死结。计算经济学范式 (Slow-Fast Hybrid Guidance): 将结构认知（Slow, 运行一次或极少调用，复杂度 $O(N)$）与状态自适应纠偏（Fast, 高频次调用，复杂度 $O(1)$）解耦。通过极轻量化的 Adapter 实现高频次更新，获得绝对的 Wall-clock time 硬件正收益。稠密信号轨迹蒸馏 (Dense Trace Distillation): 提出 Trace-Distillation 预训练机制，将蕴含图中的冲突驱动路径和低 LBD 子句等高价值领域知识转化为稠密的自监督信号，有效克服了传统强化学习在 SAT 领域的冷启动与奖励稀疏难题。📐 2. 架构设计与前向传播 (Architecture & Forward)整个设计的灵魂在于：用高复杂度的静态拓扑特征进行基底初始化，再用极轻量的门控机制结合动态事件进行低开销、高频次的动态纠偏。[h_x] (静态嵌入) ───►  [ 门控融合机制 (Gated Fusion) ] ───► [混合表征 m_x] ───► MLP_head ───► Δ Guidance
                          ▲
[e_t(x) || g_t] ──► MLP_event (动态映射)

SBE-Adapter 前向传播数学流动态事件语义映射 (Dynamic Event Mapping):对于变量 $x$ 在 $t$ 轮次的动态特征 $\mathbf{e}_t(x)$ 与全局求解状态 $\mathbf{g}_t$，通过单层线性层与激活函数映射到与 GNN 隐藏特征相同的维度 $D$：$$\mathbf{f}_{\text{dynamic}}(x) = \text{LeakyReLU}\left(\mathbf{W}_{\text{ev}} [\mathbf{e}_t(x) \,\|\, \mathbf{g}_t] + \mathbf{b}_{\text{ev}}\right)$$门控自适应融合 (Symmetry-Breaking Gate):设计一个动态门控权重 $\mathbf{g}_t(x) \in [0, 1]^D$，该权重由静态语义 $\mathbf{h}_x$ 与动态轨迹 $\mathbf{f}_{\text{dynamic}}(x)$ 共同决定。即使两个变量完全同构（即静态嵌入 $\mathbf{h}_{x_1} = \mathbf{h}_{x_2}$），因其搜索轨迹差异（$\mathbf{e}_t(x_1) \neq \mathbf{e}_t(x_2)$），最终的门控也会输出不同的混合表征 $\mathbf{m}_x$：$$\mathbf{g}_t(x) = \sigma\left(\mathbf{W}_{\text{gate}} [\mathbf{h}_x \,\|\, \mathbf{f}_{\text{dynamic}}(x)] + \mathbf{b}_{\text{gate}}\right)$$$$\mathbf{m}_x = \mathbf{g}_t(x) \odot \mathbf{h}_x + (\mathbf{1} - \mathbf{g}_t(x)) \odot \mathbf{f}_{\text{dynamic}}(x)$$其中 $\sigma$ 为 Sigmoid 函数，$\odot$ 为 Hadamard 乘积（逐元素相乘）。Guidance 修正量输出 (Multi-Head Output):利用线性投影层预测变量权重与极性的增量（或残差）：$$\begin{bmatrix} \Delta \log w_t(x) \\ \Delta \text{logit } p_t(x) \end{bmatrix} = \mathbf{W}_{\text{head}} \mathbf{m}_x + \mathbf{b}_{\text{head}}$$引导更新机制 (Guidance Update):$$\log w_t(x) = \text{clip}\left(\log w_0(x) + \Delta \log w_t(x), \tau_{\min}, \tau_{\max}\right)$$$$p_t(x) = \sigma\left( \text{logit}(p_0(x)) + \Delta \text{logit } p_t(x) \right)$$📊 3. 无量纲化动态特征矩阵 (Event State $e_t(x)$)⚠️ 去漂移原则: 严禁直接使用未经处理的累计数值，必须通过排名 (Percentile) 与速率 (Rate) 消除跨实例、跨轮次的尺度漂移。| 特征类别 | 具体特征项 | 物理意义与无量纲化处理 || 绝对活跃度 | log1p(activity)  log1p(conflict_involvement) | 经典的 VSIDS 活跃度与近期冲突参与度。 || 相对排名 (关键) | percentile(activity)  percentile(conflict_involvement) | 当前变量在全实例所有变量中的排名（0~1 浮点数）。解决跨规模实例绝对值漂移的核心。 || 短期增量 (Delta) | $\Delta \text{decisions} / \text{rollout\_conflicts}$  $\Delta \text{propagations} / \text{rollout\_conflicts}$ | 当前这一轮 Rollout 区间内，该变量被主动决策和被动蕴含的速率。 || 极性倾向 (文字级) | $\frac{\text{pos\_conf} - \text{neg\_conf}}{\text{pos\_conf} + \text{neg\_conf} + \epsilon}$ | 直接量化该变量在正负文字冲突时的不平衡度，范围 $[-1, 1]$，直接指导极性分支。 || 赋值状态 | assigned_ratio  last_polarity | 变量被赋值的时间比例，以及上一次被赋予的真值极性。 |🔄 4. 三阶段渐进式训练流 (Training Pipeline)阶段 1: RLAF 初始化 ──► 阶段 2: Trace Distillation (监督预训练) ──► 阶段 3: Solver-in-loop GRPO (微调)

阶段 1：RLAF 初始化冻结一个训练好的 RLAF GNN 模型。输入原始 CNF 公式，运行单次前向传播，提取全局拓扑特征 $\mathbf{h}_x$，并输出基础引导权重 $w_0$ 与基础极性 $p_0$。该静态嵌入在后续求解过程中保持不变。阶段 2：Trace Distillation (轨迹描述隐式监督)该阶段旨在利用无监督的求解轨迹快速赋予 Adapter 基础的物理常识（即预测哪些变量有助于推进求解）：LBD 预测目标: 运行 Solver，收集 Rollout 中衍生出的低 LBD（$\le 3$）优质子句。若变量 $x$ 频繁出现在这些子句中，设定标签 $y_{\text{val}}(x) = 1$，否则为 $0$。决策路径预测目标: 回溯蕴含图 (Implication Graph)，提取直接导致冲突发生 (conflict-driving) 的决策变量序列，设定标签 $y_{\text{dec}}(x) = 1$，否则为 $0$。自监督 Loss 优化:$$\mathcal{L}_{\text{distill}} = \text{BCE}(y_{\text{val}}(x), \hat{y}_{\text{val}}(x)) + \lambda \text{BCE}(y_{\text{dec}}(x), \hat{y}_{\text{dec}}(x))$$阶段 3：Solver-in-loop GRPO 微调使用组相对策略优化 (GRPO)，对同一个 CNF 采样一组（如 $K=4$）不同的 Adapter 修正扰动。在真实 Solver 中运行，计算相对 Reward：$$\text{Reward} = - \left( \alpha \log1p(\text{Total\_Wall\_Clock\_Time}) + \beta \log1p(\text{Conflicts}) \right)$$💡 工程防御机制: 若实例在 Budget 内未解出（Timeout），引入基于变量消减进度的阶梯惩罚：$\text{Reward}_{\text{timeout}} = - \text{Max\_Time} \times (1.0 + \text{Remaining\_Variables\_Ratio})$。🧪 5. 严格消融与压力测试矩阵 (Ablation & Stress Test)5.1 评估指标规范 (Total Wall-clock Accounting)所有的性能评估指标必须严格遵循 Wall-clock Time 累计。不可为了突显性能而剔除模型推理时间：$$\text{Time}_{\text{total}} = \text{Time}_{\text{GNN\_init}} + \sum_{t=1}^{T} \left( \text{Time}_{\text{rollout}}^{(t)} + \text{Time}_{\text{adapter}}^{(t)} \right) + \text{Time}_{\text{final\_solve}}$$5.2 消融对照实验Baseline 1 (Core): 纯传统求解器（Glucose / Kissat 默认配置）。Baseline 2 (One-shot GNN): 仅包含阶段 1 的静态引导，后续不更新。Variant A (No-Symmetry-Breaking): 剥离门控融合机制，改为常规的特征 Concat 后直接输入 MLP。Variant B (No-Trace-Pretrain): 跳过阶段 2 的轨迹蒸馏，直接用强化学习在稀疏奖励下硬刷 Adapter。Our Full Method: 包含完整三阶段闭环的 SBE-Adapter 引导求解器。5.3 对称性压力测试数据集 (Symmetry Stress Tests)用于针对性验证 Search-induced symmetry breaking 机制在突破 $1\text{-WL}$ 表达力上限时的有效性：Tseitin Formulas（矛盾公式，高度规整同构，用以卡死传统静态 GNN）Pigeonhole Principle (PHP)（鸽巢原理，极端对称）Regular Graph Coloring（正则图着色问题）



EchoSAT: 基于求解轨迹蒸馏的快慢协同 CDCL SAT 求解引导方法📑 1. 核心贡献 (Core Contributions)在将图神经网络 (GNN) 引入 CDCL SAT 求解器的研究中，现有的神经求解器普遍面临三大核心瓶颈：静态图神经网络表达力不足（受限于 $1\text{-WL}$ 检验而无法自发打破高度对称/同构图的对称性）、GPU 频繁推理带来的巨大 Wall-clock time 延迟开销，以及强化学习（RL）在 SAT 求解中极其稀疏且延迟的奖励机制。为此，EchoSAT 提出了一种全新的 Slow-Fast 神经引导框架，其核心学术贡献如下：打破表达力上限 (Search-Induced Symmetry Breaking): 首次提出利用 Solver 动态搜索轨迹 (Execution Trace) 作为天然、廉价的动态标识符 (Dynamic Identifiers)。由于对称变量在搜索树中所处的动态上下文不同，通过轨迹特征可以自发且动态地打破静态图的同构死结，在数学上规避了静态消息传递 GNN 无法区分同构图的缺陷。计算经济学范式 (Slow-Fast Hybrid Guidance): 将高维度的拓扑认知（Slow，复杂度 $O(N)$，在求解开始时运行一次或极少调用）与低维度的状态自适应纠偏（Fast，复杂度 $O(1)$，高频次调用）解耦。通过极轻量化的 Adapter 实现高频次、零显存开销的动态更新，获得绝对的 Wall-clock time 硬件正收益。稠密信号轨迹蒸馏 (Dense Trace Distillation): 提出 Trace-Distillation 预训练机制。我们将蕴含图中的冲突驱动路径和低 LBD 子句等高价值领域知识，转化为对变量或文字的稠密自监督信号，有效克服了传统强化学习在 SAT 领域的冷启动与奖励稀疏难题。📐 2. 架构设计与前向传播 (Architecture & Forward)整个设计的灵魂在于：用高复杂度的静态拓扑特征进行基底初始化，再用极轻量的门控机制结合动态事件进行低开销、高频次的动态纠偏。[h_x] (静态嵌入) ───►  [ 门控融合机制 (Gated Fusion) ] ───► [混合表征 m_x] ───► MLP_head ───► Δ Guidance
                          ▲
[e_t(x) || g_t] ──► MLP_event (动态映射)
SBE-Adapter 前向传播数学流动态事件语义映射 (Dynamic Event Mapping):对于变量 $x$ 在 $t$ 轮次的动态特征 $\mathbf{e}_t(x)$ 与全局求解状态 $\mathbf{g}_t$，通过单层线性层与非线性激活函数映射到与 GNN 隐藏特征相同的维度 $D$：$$\mathbf{f}_{\text{dynamic}}(x) = \text{LeakyReLU}\left(\mathbf{W}_{\text{ev}} [\mathbf{e}_t(x) \,\|\, \mathbf{g}_t] + \mathbf{b}_{\text{ev}}\right)$$门控自适应融合 (Symmetry-Breaking Gate):设计一个动态门控权重 $\mathbf{g}_t(x) \in [0, 1]^D$，该权重由静态语义 $\mathbf{h}_x$ 与动态轨迹 $\mathbf{f}_{\text{dynamic}}(x)$ 共同决定。即使两个变量完全同构（即静态嵌入 $\mathbf{h}_{x_1} = \mathbf{h}_{x_2}$），因其搜索轨迹差异（$\mathbf{e}_t(x_1) \neq \mathbf{e}_t(x_2)$），最终的门控也会输出不同的混合表征 $\mathbf{m}_x$，从而动态打破对称性：$$\mathbf{g}_t(x) = \sigma\left(\mathbf{W}_{\text{gate}} [\mathbf{h}_x \,\|\, \mathbf{f}_{\text{dynamic}}(x)] + \mathbf{b}_{\text{gate}}\right)$$$$\mathbf{m}_x = \mathbf{g}_t(x) \odot \mathbf{h}_x + (\mathbf{1} - \mathbf{g}_t(x)) \odot \mathbf{f}_{\text{dynamic}}(x)$$其中 $\sigma$ 为 Sigmoid 函数，$\odot$ 为 Hadamard 乘积（逐元素相乘）。Guidance 修正量输出 (Multi-Head Output):利用线性投影层预测变量权重与极性的增量（或残差）：$$\begin{bmatrix} \Delta \log w_t(x) \\ \Delta \text{logit}(p_t(x)) \end{bmatrix} = \mathbf{W}_{\text{head}} \mathbf{m}_x + \mathbf{b}_{\text{head}}$$引导更新机制 (Guidance Update):根据增量实时更新当前的决策权重（Decision Weight）与极性（Polarity）：$$\log w_t(x) = \text{clip}\left(\log w_0(x) + \Delta \log w_t(x), \tau_{\min}, \tau_{\max}\right)$$$$p_t(x) = \sigma\left( \text{logit}(p_0(x)) + \Delta \text{logit}(p_t(x)) \right)$$📊 3. 无量纲化动态特征矩阵 (Event State $\mathbf{e}_t(x)$)⚠️ 去漂移原则: 严禁直接使用未经处理的累计数值，必须通过排名 (Percentile) 与速率 (Rate) 消除跨实例、跨轮次的尺度漂移。特征类别具体特征项物理意义与无量纲化处理绝对活跃度$\text{log1p}(\text{activity})$$\text{log1p}(\text{conflict\_involvement})$经典的 VSIDS 活跃度与近期冲突参与度，采用对数变换缓和极端极值。相对排名 (关键)$\text{percentile}(\text{activity})$$\text{percentile}(\text{conflict\_involvement})$当前变量在全实例所有变量中的百分位排名（$0 \le x \le 1$）。解决跨规模实例绝对值漂移的核心特征。短期速率 (Rate)$\Delta \text{decisions} / \text{rollout\_conflicts}$$\Delta \text{propagations} / \text{rollout\_conflicts}$当前这一轮 Rollout 区间内，该变量被主动决策和被动蕴含的速率（单位 Conflict 内的变化率）。极性倾向 (文字级)$\frac{\text{pos\_conf} - \text{neg\_conf}}{\text{pos\_conf} + \text{neg\_conf} + \epsilon}$文字级特征下沉。直接量化该变量在正负文字冲突时的不平衡度，范围 $[-1, 1]$，用于强监督极性分支。赋值状态$\text{assigned\_ratio}$$\text{last\_polarity}$变量被赋值的时间比例（$0 \le \text{ratio} \le 1$），以及上一次被赋予的真值极性。🔄 4. 三阶段渐进式训练流 (Training Pipeline)阶段 1: RLAF 初始化 ──► 阶段 2: Trace Distillation (监督预训练) ──► 阶段 3: Solver-in-loop GRPO (微调)
阶段 1：RLAF 初始化冻结一个预训练好的 RLAF GNN 模型（作为 Slow 编码器）。输入原始 CNF 公式，运行单次前向传播，提取全局拓扑特征 $\mathbf{h}_x$，并输出基础引导权重 $w_0$ 与基础极性 $p_0$。该静态嵌入在后续求解更新中保持不变，常驻内存。阶段 2：Trace Distillation (轨迹描述隐式监督)该阶段旨在利用无监督的求解轨迹快速赋予 Adapter 基础的领域常识（即预测哪些变量有助于推进求解）：LBD 预测目标: 运行 Solver，收集 Rollout 中衍生出的低 LBD（$\le 3$）优质子句。若变量 $x$ 频繁出现在这些子句中，设定标签 $y_{\text{val}}(x) = 1$，否则为 $0$。决策路径预测目标: 回溯蕴含图 (Implication Graph)，提取直接导致冲突发生 (conflict-driving) 的决策变量序列，设定标签 $y_{\text{dec}}(x) = 1$，否则为 $0$。自监督 Loss 优化:$$\mathcal{L}_{\text{distill}} = \text{BCE}(y_{\text{val}}(x), \hat{y}_{\text{val}}(x)) + \lambda \text{BCE}(y_{\text{dec}}(x), \hat{y}_{\text{dec}}(x))$$阶段 3：Solver-in-loop GRPO 微调使用组相对策略优化 (GRPO)，对同一个 CNF 采样一组（如 $K=4$）不同的 Adapter 修正扰动。在真实 Solver 中运行，计算相对基准的 Reward：$$\text{Reward} = - \left( \alpha \ln(1 + \text{Time}_{\text{total}}) + \beta \ln(1 + \text{Conflicts}_{\text{total}}) \right)$$💡 工程防御机制: 若实例在 Budget 内未解出（Timeout），引入基于变量消减进度的阶梯惩罚：$$\text{Reward}_{\text{timeout}} = -T_{\max} \cdot (1.0 + \rho_{\text{rem}})$$其中 $T_{\max}$ 为最大超时时间，$\rho_{\text{rem}}$ 为未消解的变量比例。🧪 5. 严格消融与压力测试矩阵 (Ablation & Stress Test)5.1 评估指标规范 (Total Wall-clock Accounting)所有的性能评估指标必须严格遵循 Wall-clock Time 累计。不可为了突显性能而剔除模型推理和框架通信的时间：$$\text{Time}_{\text{total}} = \text{Time}_{\text{GNN\_init}} + \sum_{t=1}^{T} \left( \text{Time}_{\text{rollout}}^{(t)} + \text{Time}_{\text{adapter}}^{(t)} \right) + \text{Time}_{\text{final\_solve}}$$5.2 消融对照实验 (Ablation Matrix)Baseline 1 (Core): 纯传统求解器（Glucose / Kissat 默认配置）。Baseline 2 (One-shot GNN): 仅包含阶段 1 的静态引导，后续不更新。Variant A (No-Symmetry-Breaking): 剥离门控融合机制，改为常规的特征 Concat 后直接输入 MLP（用于验证门控结构对打破对称性的必要性）。Variant B (No-Trace-Pretrain): 跳过阶段 2 的轨迹蒸馏，直接用强化学习在稀疏奖励下硬刷 Adapter（用于验证轨迹蒸馏解决冷启动和稀疏奖励的有效性）。EchoSAT (Full): 包含完整三阶段闭环与门控自适应融合的 SBE-Adapter 引导求解器。5.3 对称性压力测试数据集 (Symmetry Stress Tests)用于针对性验证 Search-induced symmetry breaking 机制在突破 $1\text{-WL}$ 表达力上限、打破高度同构/对称状态时的有效性：Tseitin Formulas（矛盾公式，高度规整同构，用以阻断传统静态 GNN 引导）。Pigeonhole Principle (PHP)（鸽巢原理，变量与子句呈现极端对称分布）。Regular Graph Coloring（正则图着色问题，节点间高度同质）。



  最需要借鉴的 5 点：

  1. “无量纲化动态特征”要作为下一步重点

     参考里强调不能直接用累计值，必须用 rank / percentile / rate。你现在已经有 log + rate + rank，但有一个问题：当前 delta_log_values = log_values，严格说还不是
     真正的“跨 rollout delta”。如果后面做多轮 refinement，这个地方必须改成：

     delta_t = event_t - event_{t-1}

     这是非常关键的，因为 400 上绝对计数容易漂移，gate/adapter 很可能学不到稳定规律。
  2. literal-level 极性事件非常值得补

     参考里提到：

     pos_conf - neg_conf / pos_conf + neg_conf

     这个点很重要。你当前 event state 是 variable-level，基本在调变量权重；但 SAT solver 里极性也很关键。400 上 always adapter solved 数比 baseline 多，但搜索量
     更高，说明“方向可能有用，但控制不准”。literal-level polarity signal 可能正好能修正这个问题。

     建议优先补：
      - event_lit_pos_conflict
      - event_lit_neg_conflict
      - event_lit_pos_propagation
      - event_lit_neg_propagation
      - polarity_conflict_bias = (pos_conf - neg_conf)/(pos_conf + neg_conf + eps)
  3. SBE-Adapter 的 gated fusion 架构值得做成新 ablation

     参考里的结构是：

     m_x = gate * h_x + (1 - gate) * f_dynamic

     你现在的 adapter 更接近：

     MLP(concat(h_x, event_state)) -> delta

     外面再乘 event gate / rho gate。

     这不是一回事。参考里的 gated fusion 更像真正的“动态轨迹改写静态表征”，理论上更适合讲 Search-induced symmetry breaking。建议不要直接替换当前主线，而是加一个
     新 variant：
      - 当前：ResidualConcatAdapter
      - 新增：SBEGatedFusionAdapter

     然后在 300/350/400 和对称数据集上做 ablation。
  4. 对称性压力测试必须借鉴

     这个是论文故事最需要的实验补强。只在 random 3SAT 上讲 “search-induced symmetry breaking” 不够硬。

     建议加小规模：
      - PHP
      - Tseitin
      - regular graph coloring

     目的不是一定要大幅赢 Glucose，而是证明：
      - 静态 GNN 对称变量输出高度相同；
      - solver event trace 后，同构变量获得不同 event identity；
      - adapter 输出开始分化；
      - 搜索行为发生变化。

     这个实验非常适合顶会审稿人。
  5. 400 方向要借鉴 timeout/progress-aware 训练思想

     参考里说 timeout 不能只给统一惩罚，要考虑剩余进度。你现在 400 上大量接近 60s，mean time 区分度很弱，solved count 更关键。

     下一步如果做 GRPO 或 gate tuning，reward 不能只看 mean time，应该加入：
      - solved / timeout binary
      - conflicts
      - decisions
      - maybe timeout penalty

     否则模型会优化小幅时间波动，但不一定提升 400 solved count。