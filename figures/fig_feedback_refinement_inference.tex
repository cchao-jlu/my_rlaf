\documentclass{article}
\usepackage[paperwidth=15in,paperheight=4.2in,margin=0.15in]{geometry}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit}
\pagestyle{empty}

\begin{document}
\begin{tikzpicture}[
    font=\small,
    node distance=8mm and 10mm,
    block/.style={draw, rounded corners=2pt, align=center, minimum height=8mm, minimum width=27mm, fill=blue!5},
    solver/.style={draw, rounded corners=2pt, align=center, minimum height=8mm, minimum width=28mm, fill=red!6},
    state/.style={draw, rounded corners=2pt, align=center, minimum height=8mm, minimum width=30mm, fill=orange!10},
    out/.style={draw, rounded corners=2pt, align=center, minimum height=8mm, minimum width=27mm, fill=green!8},
    arrow/.style={-{Latex[length=2.2mm]}, thick},
    smallarrow/.style={-{Latex[length=2mm]}, thick}
]

\node[block] (graph) {CNF graph\\$G$};
\node[block, right=of graph] (gnn0) {GNN\\$z=0$};
\node[out, right=of gnn0] (g0) {Initial\\guidance $a_0$};
\node[solver, right=of g0] (warmup) {SAT solver\\warmup};
\node[state, right=of warmup] (stats) {Solver stats\\$s_0$};

\node[state, below=12mm of stats] (encode) {Feedback encoder\\$z=\phi(s_0)$};
\node[block, left=of encode] (gnn1) {GNN\\conditioned on $z$};
\node[out, left=of gnn1] (g1) {Refined\\guidance $a_1$};
\node[solver, left=of g1] (finalsolver) {SAT solver\\final run};
\node[out, left=of finalsolver] (final) {Result and\\statistics};

\draw[arrow] (graph) -- (gnn0);
\draw[arrow] (gnn0) -- (g0);
\draw[arrow] (g0) -- (warmup);
\draw[arrow] (warmup) -- (stats);
\draw[arrow] (stats) -- (encode);
\draw[arrow] (encode) -- (gnn1);
\draw[arrow] (gnn1) -- (g1);
\draw[arrow] (g1) -- (finalsolver);
\draw[arrow] (finalsolver) -- (final);

\draw[smallarrow] (graph.south) |- node[pos=0.25, left] {$G$ reused} (gnn1.south);

\node[draw, rounded corners=2pt, fit=(graph)(gnn0)(g0)(warmup)(stats), inner sep=4mm,
      label={[font=\small]above:First pass and solver feedback}] {};
\node[draw, rounded corners=2pt, fit=(encode)(gnn1)(g1)(finalsolver)(final), inner sep=4mm,
      label={[font=\small]below:Feedback refinement pass}] {};

\end{tikzpicture}
\end{document}
